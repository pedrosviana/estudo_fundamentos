### Using arrays in JS
- In a brief definition an array is a list of something;
- It's represented by [ ]
- It may has a defined or variable size and also can be resized easily;

-------
### Built-in methods when working with arrays
- How do we manipulate, insert and retrieve data

`let numbers = [1,2,3,4,5]`
- To access the methods we're gonna use the the array name and .

numbers.push()
numers.pop()

